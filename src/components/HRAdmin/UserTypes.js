import React from "react";
import { Button } from "antd";

const UserTypes = () => {
  return (
    <div>
      <h2>UserTypes</h2>
      <Button>Add User Type</Button>
    </div>
  );
};

export default UserTypes;
